{
  "name": "check-square-number",
  "version": "1.0.0",
  "description": "Check square number",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint src",
    "test": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/thangdq/check-square-number.git"
  },
  "keywords": [
    "check_numbers"
  ],
  "author": "ThangDQ",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "coveralls": "^2.11.14",
    "eslint": "^3.5.0",
    "istanbul": "^0.4.5",
    "js-beautify": "^1.6.4",
    "mocha": "^3.0.2"
  }
}
